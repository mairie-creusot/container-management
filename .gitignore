node_modules/
.pnpm-store/
dist/
build/
coverage/
*.log
*.tsbuildinfo
.env
.env.*
!.env.example
# Les gabarits *.example ne contiennent jamais de secret et doivent rester versionnés (le motif
# .env.* ci-dessus les excluait, ex: deploy/compose/.env.prod.example).
!*.example
.DS_Store

# données de dev locales (config assistant, clone GitOps)
apps/api/data/
data/

# clés privées de signature des modules (scripts/sign-plugin.mjs) — une clé privée ne doit jamais
# être versionnée, ni se retrouver sur un serveur : seule la clé PUBLIQUE (*.pub) se partage
*.key
*.pem

# rust / wasm-pack
target/
packages/wasm-core/pkg/

# editors
.vscode/*
!.vscode/extensions.json
.idea/
